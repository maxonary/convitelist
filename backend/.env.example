# =============================================================================
# Backend Configuration
# =============================================================================

# Backend server port (default: 3001)
PORT=3001

# Frontend/Client URL (used for CORS configuration)
# In production, set this to your actual frontend URL (e.g., https://yourdomain.com)
CLIENT_URL="http://localhost:3000"

# =============================================================================
# Database Configuration
# =============================================================================

# Database connection string
# For PostgreSQL:
# DATABASE_URL="postgresql://username:password@host:port/database-name"
#
# For SQLite (recommended for single-server deployment):
DATABASE_URL="file:./database.db"

# =============================================================================
# Security Secrets - CRITICAL: Generate strong random values!
# =============================================================================

# JWT Secret: Used for signing JSON Web Tokens for admin authentication
# REQUIRED: Must be set to a secure random string (minimum 32 characters)
# Generate with: openssl rand -base64 32
# NEVER use default values in production!
JWT_SECRET="change-this-to-a-very-secure-random-secret-key"

# Session Secret: Used for encrypting session cookies
# REQUIRED: Must be set to a secure random string (minimum 32 characters)
# Generate with: openssl rand -base64 32
# NEVER use default values in production!
SESSION_SECRET="change-this-to-a-very-secure-random-secret-key"

# =============================================================================
# Minecraft RCON Configuration - SECURITY CRITICAL
# =============================================================================

# RCON Host: MUST be 127.0.0.1 (localhost) for security!
# SECURITY WARNING: RCON uses plaintext passwords. Only connect via localhost
# when backend and Minecraft server are on the same system.
# DO NOT expose RCON port to the internet!
MINECRAFT_RCON_HOST="127.0.0.1"

# RCON Port: Default Minecraft RCON port (typically 25575)
# Must match the rcon.port setting in your server.properties file
MINECRAFT_RCON_PORT="25575"

# RCON Password: Strong password for RCON authentication
# REQUIRED: Use a strong, unique password (minimum 16 characters)
# Generate with: openssl rand -base64 24
# Must match the rcon.password setting in your server.properties file
# SECURITY: This password is sent in plaintext - only use on localhost!
MINECRAFT_RCON_PASSWORD="change-this-to-a-strong-random-password"

# =============================================================================
# Security Best Practices
# =============================================================================
# 1. Generate all secrets using: openssl rand -base64 32 (or 24 for RCON password)
# 2. Set MINECRAFT_RCON_HOST to 127.0.0.1 (never expose RCON to internet)
# 3. Configure firewall to block RCON port (25575) from external access
# 4. Use HTTPS in production (configure SSL/TLS certificates)
# 5. Never commit .env file to version control (already in .gitignore)
# 6. Rotate secrets periodically
# 7. See SECURITY.md and README.md for detailed security information